@AGENTS.md

## Claude Code Notes

- Keep this file as a thin Claude-specific adapter. Put shared guidance in `AGENTS.md`.
- Add project skills under `.claude/skills/` only when a repeated procedure is too long or conditional for `AGENTS.md`.
- Use `/memory` if loaded instructions or auto memory behavior needs inspection.
